import "tfplan/v2" as tfplan

s3_being_created = filter tfplan.resource_changes as _, rc {
    rc.type is "aws_s3_bucket" and
    rc.change.actions contains "create"
}

main = rule {
    all s3_being_created as _, rc {
        true
    }
}